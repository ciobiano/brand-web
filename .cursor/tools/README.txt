playwright.json registers a single MCP tool named "plat4m-scroll".
It runs the plat4mScrollTriggerInspector script with --save and --summary
from the workspace root so the generated reports land in scripts/../reports.
